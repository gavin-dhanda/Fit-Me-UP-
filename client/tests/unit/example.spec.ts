import { expect, test } from "vitest";

test("1 + 2 should be 3", () => {
  expect(1 + 2).toBe(3);
});

test('Annie is the best', () => {
  let AnnieIsTheBest : boolean = true;
  expect(AnnieIsTheBest).toBe(true);
})
